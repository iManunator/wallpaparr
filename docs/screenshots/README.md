# Screenshots

SVG **placeholders** ship in git so the README renders on GitHub without committing multi‑MB PNGs. Each file is labeled as a placeholder in the artwork.

Replace with 1280×720 PNG captures from a real session after `./scripts/verify.sh` (same filenames, or update README `src=`):

| File | Shot |
| --- | --- |
| `logo.svg` | Wordmark |
| `tonight.svg` | Tonight page — 16:9 pick preview with Projectivy chrome |
| `gallery.svg` | Queue badges + pin / never-show |
| `dashboard.svg` | Health cards + providers + queues |
